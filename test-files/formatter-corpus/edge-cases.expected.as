void Render()
{
  DoSomethingLong(
    alpha,
    beta,
    gamma,
    delta,
    epsilon
  );
  obj.
    Manager.
    Component.
    Run().
    Apply();
  auto keep = cond ? veryLongValue.WithChain().DoThing() : fallback.WithChain().DoThing();
}